#include "main.h"
/**
 * main - prints numbers (0 to 9)
 * @n is number
 *Return : always 0
 */
void print_numbers(void);
{
	for (n >= 0; n <= 9; n++)
	{
		_putchar("%d", n);
	}
	_putchar("\n");
	return (0);
}
